export default function HomePage() {
  return (
    <main style={{ padding: "48px", fontFamily: "system-ui, sans-serif" }}>
      <h1>Unisphere</h1>
      <p>College discovery and admissions intelligence platform.</p>
      <p>API routes are available at <code>/api/colleges</code> and <code>/api/predict</code>.</p>
    </main>
  );
}
