# Copy this file to `.env` for local development or use the same values in Vercel environment variables.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require"

# Optional: add auth provider secrets here when you wire authentication.
# NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET="replace-me"
